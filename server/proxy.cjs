// Proxy disabled: upstream PSX usage is turned off while the project
// operates in demo mode. This placeholder keeps the script present but
// prevents accidental forwarding to external hosts.

// Proxy removed per user request. This file is now a no-op stub.
console.log('Proxy removed: stubbed out to prevent accidental proxying to PSX.');
process.exit(0);
